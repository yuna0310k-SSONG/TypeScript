// 모든타입 가능
// const a1: any = [{ name: "유나" }, { age: 21 }];

// 절대 존대하지 않는 타입
// const a2:never =

// Function 타입
// const a3: Function = () => 123;

// const sum = (x: number, y: number) => x + y;

//const a4: unknown; -> 아직 타입이 정의되지 않은 상태

// 불리언 화
// const c1 = (num: number): boolean => !!num;

// 문자화
//const c2 = (num: number): string => String(num);
